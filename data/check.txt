required	必須	1		空欄あるいは未選択の場合にエラーを出したいときに指定します。
alt	別名		12	表示用の項目名をname属性とは別にしたい場合に指定します。
attach	添付	1		添付ファイルのアップロードフィールドのとき必ずチェックしてください。
trim	除	1		改行コードをすべて除去します。
trim2	除2	1		両端の改行コード、半角スペース、全角スペースおよびタブを除去します。
z2h	→半	1		全角英数字を半角に変換します。
d_only	数	1		半角数字以外を入力するとエラーを出したいときに指定します。
h2z	→全	1		半角英数字を全角に変換します。
h2z_kana	→全ｶ	1		半角カタカナを全角に変換します。
kata_only	ア	1		カタカナ以外を入力するとエラーを出したいときに指定します。
hira_only	あ	1		ひらがな以外を入力するとエラーを出したいときに指定します。
deny_rel	依	1		機種依存文字を拒否したいときに指定します。
url	URL	1		URLのフォーマットチェックをしたいときに指定します。
email	ﾒｰﾙ	1		メールアドレスのフォーマットチェックをしたいときに指定します。
compare	照合	1		照合対象のフィールドの値とデータが一致しているかチェックしたいときに指定します。EMAILとEMAIL2など。
min	最小		2	文字列のとき最小文字(バイト)数(len_min)／checkbox/selectのとき最小選択数(num_min)／数値のとき(d_onlyにチェック)最小(min)を指定します。
max	最大		2	文字列のとき最大文字(バイト)数(len_max)／checkbox/selectのとき最大選択数(num_max)／数値のとき(d_onlyにチェック)最大(max)を指定します。
regex	正規(真でｴﾗｰ)		7	マッチするとエラーになるように正規表現を指定できます(※Perl文法の知識がいります)。
regex2	正規2(偽でｴﾗｰ)		7	マッチしないとエラーになるように正規表現を指定できます(※Perl文法の知識がいります)。
